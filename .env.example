# ─────────────────────────────────────────────────────────────────────────────
# Template file – safe to commit.
# Copy this to .env and fill in your real values for local development.
# For GitHub Actions, add each key as a Secret in:
#   Repo → Settings → Secrets and variables → Actions → New repository secret
# ─────────────────────────────────────────────────────────────────────────────

# Telegram credentials (required)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Email credentials for daily summary (optional)
EMAIL_ADDRESS=
EMAIL_PASSWORD=

# Execution window (optional – defaults shown, HH:MM, UTC)
EXECUTION_START_TIME=08:00
EXECUTION_STOP_TIME=16:00

# Alert window (optional – defaults shown, HH:MM, UTC)
ALERT_WINDOW_START_TIME=09:30
ALERT_WINDOW_END_TIME=15:30
ALERT_INTERVAL_MINUTES=60

# Message templates (optional)
STARTUP_SIGNAL_TEMPLATE=✅ Alert app started at {timestamp} | execution window: {execution_start}-{execution_stop} | alert window: {alert_start}-{alert_end}
ALERT_MESSAGE_TEMPLATE=Alert at {timestamp}
SUMMARY_SUBJECT_TEMPLATE=Daily Alert Summary - {date}

# Manual test mode (optional)
MANUAL_TEST_MODE=false
MANUAL_TEST_MESSAGE_TEMPLATE=🧪 Manual test signal at {timestamp} (window checks bypassed)
